# objektmarket - K-pop NFT 卡片收藏管理系統

> 🃏 一個基於 Django 的 K-pop NFT 卡片收藏管理平台

![Django](https://img.shields.io/badge/Django-5.2-green?style=flat&logo=django)
![Python](https://img.shields.io/badge/Python-3.8+-blue?style=flat&logo=python)
![SQLite](https://img.shields.io/badge/SQLite-Database-lightgrey?style=flat&logo=sqlite)

## 📖 專案簡介
這是一個基於 Django 的網頁應用程式，專門用於管理 K-pop 收藏卡片（objekt）。系統不僅可以追蹤藝人、成員、季度、收藏系列以及個別的 objekt 卡片，還包含用戶認證系統、卡片列表管理功能和篩選搜尋功能。

## 專案特色

- 🎨 **卡片系統** - 展示 NFT (objekt) 卡面圖片
- 🔍 **智能篩選** - 基於藝人、季度、收藏系列等條件進行篩選
- 👤 **用戶系統** - 完整的註冊、登入和個人資料管理
- 🃏 **管理清單** - 用戶可建立和分享 objekt 收藏清單
- 🌐 **區塊鏈整合** - 支援錢包地址和區塊鏈令牌追蹤（待開發）

## 🚀 線上展示
[Live Demo](https://objekt-marketplace.onrender.com/)

## 技術架構

### 核心技術
- **後端框架**: Django 5.2
- **資料庫**: SQLite（開發環境）
- **篩選系統**: django-filters
- **前端**: HTML5, CSS3

### 資料模型
- `Artist` - K-pop 團體/藝人
- `Member` - 隸屬於藝人的個別成員（含成員編號和代碼）
- `Season` - objekt 發行的時期劃分（含日期範圍和前綴）
- `Class` - 稀有度/等級分類（依藝人分組）
- `Collection` - 卡片編號系統
- `ObjektType` - 卡片設計/模板（連接所有上述模型）
- `ObjektCard` - 具有唯一 token_id 和持有者地址的個別卡片
- `UserProfile` - 擴展用戶模型（含錢包地址和聯絡資訊）
- `ObjektList` - 用戶建立的市場交易清單

## 快速開始

### 環境需求
- Python 3.8+
- Django 5.2
- SQLite

### 安裝步驟

1. **複製專案**
   ```bash
   git clone <repository-url>
   cd objektmarket
   ```

2. **安裝依賴**
   ```bash
   pip install django django-filters
   ```

3. **資料庫遷移**
   ```bash
   python manage.py migrate
   ```

4. **創建超級用戶**（可選）
   ```bash
   python manage.py createsuperuser
   ```

5. **啟動開發服務器**
   ```bash
   python manage.py runserver
   ```

6. **訪問應用**
   - 主頁面: http://127.0.0.1:8000/
   - 用戶註冊: http://127.0.0.1:8000/register/
   - 用戶登入: http://127.0.0.1:8000/login/
   - 市場頁面: http://127.0.0.1:8000/marketplace/
   - 管理後台: http://127.0.0.1:8000/admin/

## 常用指令

### 基本 Django 指令
```bash
python manage.py runserver          # 啟動開發服務器
python manage.py migrate            # 應用資料庫遷移
python manage.py makemigrations     # 創建新的遷移文件
python manage.py createsuperuser    # 創建管理員用戶
python manage.py shell              # 打開 Django shell
```

### 資料庫管理
```bash
python manage.py flush              # 清空資料庫數據
python manage.py loaddata <fixture> # 載入測試數據
```

## 專案結構

```
objektmarket/
├── config/                   # Django 設定檔案
│   ├── settings.py           # 主要設定（含 ngrok 主機設定）
│   └── urls.py               # 根 URL 配置（含認證路由）
├── apps/
│   ├── objekt/               # 卡片管理應用程式
│   │   ├── models.py         # 資料模型（7個核心模型）
│   │   ├── views.py          # 視圖邏輯
│   │   ├── filters.py        # 篩選器
│   │   ├── urls.py           # URL 路由
│   │   └── utils.py          # 工具函數
│   ├── accounts/             # 用戶帳號管理
│   │   ├── models.py         # UserProfile 模型
│   │   ├── views.py          # 註冊和帳號管理
│   │   ├── forms.py          # 表單定義
│   │   └── urls.py           # 用戶相關路由
│   └── marketplace/          # 市場交易功能
│       ├── models.py         # ObjektList 交易模型
│       ├── views.py          # 市場視圖
│       ├── forms.py          # 交易表單
│       └── urls.py           # 市場路由
├── templates/                # HTML 模板
│   ├── base.html             # 基礎模板（含導航）
│   ├── registration/         # 認證模板
│   │   ├── login.html        # 登入頁面
│   │   └── register.html     # 註冊頁面
│   ├── objekt/
│   │   └── list.html         # objekt 列表頁面
│   ├── accounts/
│   │   └── info.html         # 用戶資訊頁面
│   └── marketplace/          # 市場相關模板
│       ├── market.html       # 市場首頁
│       ├── mylists.html      # 我的清單
│       └── details.html      # 詳細資訊
├── static/                   # 靜態文件
│   ├── base.css              # 基礎樣式
│   ├── list.css              # 列表頁樣式
│   ├── login.css             # 登入頁樣式
│   ├── market.css            # 市場頁樣式
│   ├── info.css              # 資訊頁樣式
│   ├── details.css           # 詳細頁樣式
│   ├── mylists.css           # 清單頁樣式
│   └── favicon.ico           # 網站圖標
├── clean_traits.py           # 資料清理工具
├── import_sample_objekttype.py # 範例資料匯入
└── db.sqlite3                # SQLite 資料庫
```

## 功能說明

### 用戶認證系統
- 用戶註冊與登入功能
- UserProfile 擴展模型支援錢包地址
- 登入/登出重導向設定
- 個人資訊管理頁面

### 篩選搜尋系統
- 支援基於藝人的動態篩選
- 自動提交表單功能
- 使用 select_related() 優化資料庫查詢效能

### 卡片列表管理功能
- 用戶可建立 objekt 收藏清單
- 支援公開/私人清單設定
- 統計分析功能（藝人、季度、等級分佈）
- ManyToMany 關係管理 objekt 組合

### 模板系統
- 採用 Django 模板繼承模式
- `base.html` 提供通用結構和導航
- 模組化 CSS 設計，每個功能頁面獨立樣式

### 資料關係
```
Artist → Members → ObjektTypes
Season/Collection → ObjektTypes → ObjektCards
User → UserProfile (OneToOne)
User → ObjektList → ObjektTypes (ManyToMany)
```

## 開發注意事項

- 所有模型使用 Django 預設的 BigAutoField 作為主鍵
- `ObjektCard.token_id` 欄位是唯一的，代表區塊鏈令牌識別符
- 外鍵關係使用 `PROTECT` 刪除策略防止意外資料丟失（除 UserProfile 使用 CASCADE）
- Season 和 Class 模型使用 unique_together 約束確保每個藝人的唯一性
- 自定義屬性方法：`ObjektType.shortname`、`ObjektList.get_objekts_stats()`
- 支援 ngrok 部署測試，ALLOWED_HOSTS 已設定
- 使用最小化依賴原則，保持專案輕量化

## 未來規劃

- [] 用 API 自動抓取所有卡片
- [] 列表篩選搜尋功能（買、賣、卡片屬性）
- [] 個別設定每張卡片的價格
- [] 連動區塊鏈錢包帳號，一鍵移除已持有 / 已賣出的卡片
- [] 加入 JavaScript 讓卡片詳細資訊以懸浮視窗顯示

## 授權條款

本專案遵循 [MIT License](https://opensource.org/licenses/MIT)。

---

*這是一個用於學習和實踐 Django 開發的專案。*